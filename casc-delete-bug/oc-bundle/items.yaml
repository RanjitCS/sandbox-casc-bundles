removeStrategy:
  items: "none"
  rbac: "sync"

items:
  - kind: "clientController"
    displayName: "Controller Alpha"
    name: "controller-alpha"
    description: "First test controller for bug reproduction"
    properties:
      - configurationAsCode:
          bundle: "controller-alpha-bundle"
      - webSocket:
          enabled: true

  - kind: "clientController"
    displayName: "Controller Beta"
    name: "controller-beta"
    description: "Second test controller for bug reproduction"
    properties:
      - configurationAsCode:
          bundle: "controller-beta-bundle"
      - webSocket:
          enabled: true

  - kind: "clientController"
    displayName: "Controller Gamma"
    name: "controller-gamma"
    description: "Third test controller for bug reproduction"
    properties:
      - configurationAsCode:
          bundle: "controller-gamma-bundle"
      - webSocket:
          enabled: true
